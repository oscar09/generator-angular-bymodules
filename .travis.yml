language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-angular-bymodules' ]; then cd .. && eval "mv $currentfolder generator-angular-bymodules" && cd generator-angular-bymodules; fi

